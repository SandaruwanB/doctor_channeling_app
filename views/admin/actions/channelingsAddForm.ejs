<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
        <title>Suwasetha | Doctors Form</title>
        <%- include('../components/cssIncludes') %>
        <style>
            .form{
                display: flex;
                justify-content: center;
                align-items: center;
                height: 100vh;
            }
            form{
                width: 500px;
            }
            h1{
                color: gray;
            }
            label{
                text-align: left;
            }
        </style>
    </head>
    <body>
        <div class="form">
            <form action="" method="post">
                <div>
                    <h1 class="text-center">Manual Channeling</h1>
                    <p class="text-center">You can create manual channeling using this form</p>
                </div>
                <div class="mt-5 w-100">
                    <div class="form-group mt-2">
                        <label for="exampleInputPassword1">Doctor</label>
                        <select class="form-select" name="doctorId" aria-label="Default select example" required>
                            <% for (const doc of doctors){ %>
                                <option value="<%= doc.id %>">Dr.<%= doc.firstName + " " + doc.lastName %></option>
                            <% } %>
                        </select>
                    </div>
                    <div class="form-group mt-2">
                        <label for="exampleInputPassword1">Patient</label>
                        <select class="form-select" name="patientId" aria-label="Default select example" required>
                            <% for (const pat of patients){ %>
                                <option value="<%= pat.id %>"><%= pat.name + " " + pat.email %></option>
                            <% } %>
                        </select>
                    </div>
                    <div class="form-group">
                        <label for="exampleInputEmail1" class="text-start">Time</label>
                        <input type="time" name="time" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp" placeholder="time">
                    </div>
                    <div class="form-group">
                        <label for="exampleInputEmail1" class="text-start">Date</label>
                        <input type="date" name="date" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp" placeholder="date">
                    </div>
                    <div class="mt-4">
                        <button type="submit" class="btn btn-primary">Save</button>
                        <a href="/admin/channelings"><button type="button" class="btn btn-secondary">Cancel</button></a>
                    </div>                      
                </div>
            </form>
        </div>
        <%- include('../components/jsIncludes') %>
    </body>
</html>
